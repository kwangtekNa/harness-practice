# ARCHITECTURE — 구조

> 왜 이 문서가 필요한가: 파일이 어디에 살고 데이터가 어떻게 흐르는지 한 장으로 합의해, 하네스가
> 단계마다 같은 일을 다른 곳에 만들지 않게 한다. 이 문서는 프리앰블에 실려 모든 단계가 참고한다.

## 대상 프로젝트 산출물 (하네스가 단계별로 만들 것)

<!-- TODO 실습: 어떤 파일이 생기고 각자 무슨 책임인지 적으세요. 예)
  - settlement.py        정산 계산 로직 (순수 함수, 부수효과 없음)
  - tests/test_settlement.py   정산 규칙 검증 테스트
  - report.html          체결내역 CSV를 읽어 KPI를 보여주는 단일 HTML 리포트
-->

## 데이터 흐름

```
data/sample-trades.csv  ──▶  settlement.py (계산)  ──▶  report.html (표시)
            └── 규칙 근거: docs/SPEC.md
```

- 정산 수치의 단일 출처는 **`docs/SPEC.md`** 다. 코드/리포트는 여기 값만 쓴다.
- <!-- TODO 실습: 금액은 어떤 타입으로 흐르나? (float 금지 → Decimal/정수) 한 줄로 적으세요. -->

## 설계 원칙

<!-- TODO 실습: 2~3개. 예)
  - 정산 로직은 입출력이 명확한 순수 함수로 둔다(테스트·재사용 쉽게).
  - 외부 의존 최소화 — report.html 은 CDN 없이 자기완결.
-->
