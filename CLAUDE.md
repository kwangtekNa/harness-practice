# CLAUDE.md — 프로젝트 가드레일

> 이 파일은 하네스의 "헌법"입니다. 모든 단계가 작업 전에 읽고 따릅니다.
> `scripts/execute.py` 는 이 파일을 프리앰블 맨 앞에 넣어 모든 단계에 강제합니다.

## 이 저장소는 금융 정산 코드다 — 정확성이 최우선

한 줄 예시(아래 형식처럼 "상황 → 강제되는 행동" 으로 적습니다):

- 금액 계산은 절대 `float` 로 하지 않는다 — `Decimal` 또는 정수 원(₩) 단위로만 처리한다.

<!-- TODO 실습 (가드레일 4가지를 "행동" 으로 적으세요. 추상적 가치(X) / 구체적 행동(O)):
  1) 새 로직을 만들기 전, 도메인 가정이 모호하면 ___________ 한다.
  2) 기능을 추가·수정한 뒤에는 ___________ 한다 (내가 건드린 곳만이 아니라).
  3) 버그를 고칠 때는 ___________ 한다 (증상만 막지 말고).
  4) (자유) 이 프로젝트에 꼭 필요한 규칙 하나를 더 적으세요.
-->

## 기술 스택 / 제약

<!-- TODO 실습: 언어·라이브러리·금지사항을 적으세요.
  예) 언어: Python 3.11+ / 의존성 최소화 / 외부 네트워크 호출은 data/ 데이터로 대체 등 -->

## 도메인 규칙의 단일 출처

정산 규칙(수수료율·세금·반올림 등)의 권위 있는 출처는 **`docs/SPEC.md`** 다.
코드/문서에서 수치를 지어내지 말고 항상 이 파일을 근거로 삼는다.

## 변경 이력

| 날짜 | 변경 내용 | 대상 | 사유 |
|---|---|---|---|
| <!-- TODO 실습: YYYY-MM-DD --> | 초기 구성 | 전체 | 강의 실습 시작 |
